"""cartesian_manager's names Bloom relies on; the manager's own defaults, used until it answers."""

#: Bloom's own input on the manager, summed with the physical joystick and visual servoing.
TABLET_COMMAND_TOPIC = "/tablet_cartesian_command"
MANAGER_NODE = "/cartesian_manager"
