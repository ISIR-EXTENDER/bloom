# Docker

Docker assets will live here once the first backend and frontend migration slices are stable.

Initial goals:

- local development compose file
- production image for the FastAPI backend
- static frontend image or bundled serving strategy
- Extender deployment notes

