# API Client

Typed frontend client for the Bloom API.

This library is the boundary between Bloom frontend applications and the FastAPI backend. Components should depend on this client instead of scattering raw `fetch` calls across the dashboard.
